name: Historical Research Backtest

on:
  workflow_dispatch:
  push:
    branches:
      - "feature/**"
    paths:
      - "scripts/run_kaggle_baseline.py"
      - "src/horse_racing_predictions/**"
      - "pyproject.toml"

concurrency:
  group: historical-research-${{ github.ref }}
  cancel-in-progress: true

jobs:
  baseline:
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev,research]"
          pip install kagglehub

      - name: Download public historical input and run baseline
        run: |
          python scripts/run_kaggle_baseline.py

      - name: Publish research artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: kaggle-research-artifacts
          path: artifacts/*.json
          if-no-files-found: warn
          retention-days: 14
